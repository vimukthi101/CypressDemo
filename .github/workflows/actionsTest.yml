name: Actions Test

on: 
  pull_request:
    branches:
      - '**'
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  firstJob:
    runs-on: ubuntu-latest
    steps:  
      - name: First_Step
        uses: actions/checkout@v4

      - name: Second_Step
        run: echo 'Hello World'
        shell: bash

  secondJob:
    needs: firstJob
    runs-on: ubuntu-latest
    steps:
      - name: First_Step
        run: echo 'Bye'
        shell: bash
